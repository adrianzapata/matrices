# Copy to .env and adjust for local runs (docker-compose reads .env
# automatically). In a real deployment these must be set as secrets in the
# hosting platform, never committed.

JWT_SECRET=change-me-to-a-long-random-value
AUTH_USERNAME=admin
AUTH_PASSWORD=change-me
CORS_ORIGINS=*
